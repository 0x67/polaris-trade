[package]
name = "transport_afxdp"
version = "0.1.0"
description = "AF_XDP receive for Linux over a raw XSK driver with its own XDP redirect program, no libxdp"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true
readme = "README.md"
# optional git dep on observability-core (through transport_core) blocks crates.io until it is published
publish = false

[dependencies]
transport_core = { workspace = true }

[target.'cfg(target_os = "linux")'.dependencies]
libc = { workspace = true }
socket2 = { workspace = true }
tracing = { workspace = true }

[features]
default = []
observability = ["transport_core/observability"]

[dev-dependencies]
transport_core = { workspace = true, features = ["testing"] }

[lints]
workspace = true
